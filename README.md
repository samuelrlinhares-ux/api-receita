# Galeria de Receitas - Testes com Jest - Aula 40

MESMO projeto da Aula 39, agora 100% completo (com upload funcionando).
Voce escreve os TESTES!

## Rodar a API: npm run dev  (localhost:3000)
## Rodar os testes: npm test

## 18 TODOs (todos sao TESTES):
1-10.  Receita.test.ts: criar, foto, validar, fromJSON, toJSON, setters
11-18. ReceitaRepository.test.ts: criar, foto, listar, buscar, remover, erro

## Estrutura (ja conhecida!):
src/entities/Receita.ts                      -> CLASSE completa
src/entities/__tests__/Receita.test.ts       -> TESTES (TODOs)
src/models/ReceitaRepository.ts              -> REPOSITORY completo
src/models/__tests__/ReceitaRepository.test.ts -> TESTES (TODOs)
src/middlewares/upload.ts                    -> Multer completo
